{
  "name": "dell-photobooth-2025",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build completed locally'",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}